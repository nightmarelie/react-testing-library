import * as React from 'react';

function Form() {
  return (
    <form>
      <label htmlFor="email">Email</label>
      <input id="email" type="text" placeholder="email" />
    </form>
  );
}

export default Form;
